/// Application configuration containing all environment-specific settings.
class AppConfig {
  final String apiKey;
  final String baseUrl;

  const AppConfig({
    required this.apiKey,
    required this.baseUrl,
  });
}
